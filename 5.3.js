// const regex = /\-/g;

//   console.log(str.replace(regex, ));
// return str.replace(/[A-Z]/g, (g) => `-${g[0].toUpperCase()}`);

// let myString = "the-stealth-warrior";

// myString.replace(/-([a-z])/g, function (g) {
//   return g[1].toUpperCase();
// });

// return str.replace(regex, "");

// console.log(camelCased("the-stealth-warrior"));

// not working

// let myString = "the-stealth-warrior";

var str = "my-hyphen-string";
(str = /[_.]/g), camelCase(str);

// console.log(camelCased("the-stealth-warrior"));
